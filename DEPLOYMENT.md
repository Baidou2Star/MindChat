# myChat 服务端部署手册（Linux / WSL）

本文档给仓库协作者使用，目标是：

1. 在 Linux/WSL 一次性完成依赖安装与编译
2. 启动 `LLMServer/StatusServer/ChatServer/ChatServer2/ResourceServer/GateServerWin/VarifyServer`
3. 避免常见环境坑（protobuf 版本、npm 路径、Redis/MySQL 认证）

## 1. 目录与服务说明

服务端相关目录：

- `StatusServer/`
- `LLMServer/`（独立 LLM 代理服务，支持多 key/多地址池）
- `ChatServer/`
- `ChatServer2/`
- `ResourceServer/`
- `GateServerWin/`（名称保留从windows过来的）
- `VarifyServer/`（Node.js gRPC）

构建与运维文件：

- `CMakeLists.txt`
- `vcpkg.json`
- `scripts/manage_services.sh`

## 2. 环境要求

## 2.1 系统依赖

Ubuntu 和 Cmake环境

```bash
sudo apt update
sudo apt install -y build-essential cmake ninja-build pkg-config curl zip unzip tar
```

## 2.2 运行依赖

- Redis（建议 `127.0.0.1:6379`）
- MySQL（项目当前配置为 `127.0.0.1:33060`）
- Node.js + npm（Linux 版）

安装Redis和MySQL示例：

```bash
sudo apt install -y redis-server mysql-server
```

Node 推荐用 nvm：

```bash
curl -fsSL https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
source ~/.bashrc
nvm install --lts
node -v
npm -v
```

## 2.3 vcpkg

假设 vcpkg 安装在 `/home/<you>/vcpkg`。

```bash
cd /path/to/myChat
/home/<you>/vcpkg/vcpkg install --triplet x64-linux
```

依赖由 `vcpkg.json` 统一管理（Boost/gRPC/Protobuf/hiredis/mysql connector/jsoncpp 等）。

## 3. gRPC / Protobuf 注意事项

## 3.1 必须在 Linux 重新生成 pb 文件

如果遇到：

- `This file was generated by a newer version of protoc ...`

说明 `.pb.h/.pb.cc` 与当前 protobuf 头文件不匹配。需要在 Linux 下重新生成。

在每个服务目录执行：

```bash
protoc -I . \
  --cpp_out=. \
  --grpc_out=. \
  --plugin=protoc-gen-grpc=$(which grpc_cpp_plugin) \
  message.proto
```

目录：

- `ChatServer/`
- `ChatServer2/`
- `GateServerWin/`
- `StatusServer/`
- `ResourceServer/`

## 3.2 建议

- 始终使用同一套工具链（建议 vcpkg 提供的 protobuf/grpc）
- 重新生成后再 `cmake --build`

## 4. 配置要求（关键）

## 4.1 Redis

当前项目配置默认 Redis 密码是 `123456`，请确保 Redis 服务端密码一致。

配置示例（`/etc/redis/redis.conf`）：

```conf
requirepass 123456
```

应用配置并验证：

```bash
sudo service redis-server restart || sudo systemctl restart redis-server
redis-cli -h 127.0.0.1 -p 6379 -a 123456 PING
```

返回 `PONG` 才算正常。

## 4.2 MySQL

- 请保证账号、端口与 `*/config.ini` 一致
- 导入 SQL 结构和备份数据（`sql备份/`）

示例：

```bash
mysql -h 127.0.0.1 -P 33060 -u root -p < "sql备份/myChatStructure.sql"
```

## 4.3 Node / VarifyServer

在 `VarifyServer/` 安装依赖：

```bash
cd VarifyServer
npm install
```

必须确认 npm 是 Linux 路径（不是 `/mnt/...`、不是 `.exe`）。

## 5. CMake 编译流程

在仓库根目录：

```bash
cmake -S . -B build \
  -G "Unix Makefiles" \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_TOOLCHAIN_FILE=/home/<you>/vcpkg/scripts/buildsystems/vcpkg.cmake \
  -DVCPKG_TARGET_TRIPLET=x64-linux

cmake --build build -j
```

输出可执行文件：

- `build/StatusServer/StatusServer`
- `build/LLMServer/LLMServer`
- `build/ChatServer/ChatServer`
- `build/ChatServer2/ChatServer2`
- `build/ResourceServer/ResourceServer`
- `build/GateServerWin/GateServerWin`

## 6. 一键启动脚本

统一使用：`scripts/manage_services.sh`

## 6.1 命令

```bash
scripts/manage_services.sh start [build_dir]
scripts/manage_services.sh stop
scripts/manage_services.sh restart [build_dir]
scripts/manage_services.sh status
scripts/manage_services.sh logs <ServiceName>
```

常用示例：

```bash
scripts/manage_services.sh start build
scripts/manage_services.sh status
scripts/manage_services.sh logs GateServerWin
```

## 6.2 启动顺序

脚本内部顺序：

1. 检查/拉起 Redis + MySQL
2. 启动 VarifyServer
3. 启动 C++ 服务：
   - `LLMServer`
   - `StatusServer`
   - `ChatServer`
   - `ChatServer2`
   - `ResourceServer`
   - `GateServerWin`

## 6.4 新增数据库补丁

待办模块新增了 `todo_item` 表。

- 全量建库可直接执行：`sql备份/myChatStructure.sql`
- 只打增量可执行：`sql备份/patch_todo_llm.sql`

## 6.3 脚本环境变量

```bash
# 自定义依赖拉起命令
REDIS_START_CMD='service redis-server start' \
MYSQL_START_CMD='service mysql start' \
scripts/manage_services.sh start build

# VarifyServer 缺依赖时自动 npm install
VARIFY_AUTO_NPM_INSTALL=1 scripts/manage_services.sh start build

# 自定义 VarifyServer 启动命令（默认 npm run serve）
VARIFY_START_CMD='npm run serve' scripts/manage_services.sh start build
```
